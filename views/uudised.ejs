<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/assets/css/main.css">
</head>
<body>
    <h1>Klubi uudised</h1>
    
    <div class="main-container">
        <div class="table-container">
            <div class="table-row heading">
                <div class="row-item">Uued matkad</div>
            </div>
            <% uudised.forEach((uudis, index) => { %>
                <% if (index % 3 === 0) { %>
                    <div class="table-row">
                <% } %>
                <div class="table-column">
                    <div class="row-item"><img src="<%= uudis.uudisepiltUrl %>" width="150px" height="150"></div>
                    <div class="row-item"><%= uudis.pealkiri %></div>
                    <div class="row-item"><%= uudis.kokkuvote %></div>
                    <a href="<%= uudis.infolink %>"><div class="row-item readMore">Loe edasi</div></a>
                </div>
                <% if ((index + 1) % 3 === 0) { %>
                    </div>
                <% } %>
            <% }) %>
            <% if (uudised.length % 3 !== 0) { %>
                </div>
            <% } %>
    </div>
    
    
</body>
</html>